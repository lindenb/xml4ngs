


#foreach($genotypeMethod in ${enabledGenotypeMethods})

#sample_vcf_genotyper($sample ${genotypeMethod}) : #foreach($chromGroup in ${chromosomeGroups.keySet()}) \
	#sample_vcf_genotyper_chrom($sample ${genotypeMethod} $chromGroup) #end
	
#parse("p2m.merge.vcfs.mod.vm")

#end

#foreach($chromGroup in ${chromosomeGroups.keySet()})

#sample_vcf_genotyper_chrom($sample "samtools" $chromGroup): $(call indexed_bam, #sample_recal_chrom( ${sample} $chromGroup) ) \
	#XCAPTURE_CHROM($chromGroup) \
	$(REF)
#parse("p2m.calling.samtools.mod.vm")


#sample_vcf_genotyper_chrom($sample "gatk" $chromGroup): $(call indexed_bam, #sample_recal_chrom( ${sample} $chromGroup) ) \
	#XCAPTURE_CHROM($chromGroup) \
	$(REF)
#parse("p2m.calling.gatk.mod.vm")


#sample_vcf_genotyper_chrom($sample "gatkHapCaller" $chromGroup): $(call indexed_bam, #sample_recal_chrom( ${sample} $chromGroup) ) \
	#XCAPTURE_CHROM($chromGroup) \
	$(REF)
#parse("p2m.calling.hapcaller.mod.vm")


#sample_vcf_genotyper_chrom($sample "freebayes" $chromGroup): $(call indexed_bam, #sample_recal_chrom( ${sample} $chromGroup) ) \
	#XCAPTURE_CHROM($chromGroup) \
	$(REF)
#parse("p2m.calling.freebayes.mod.vm")



#set($genotyped_samples=[])#set($_ignoreBool=$genotyped_samples.add(${sample.name}))
#sample_vcf_genotyper_chrom($sample "varscan" $chromGroup): $(call indexed_bam, #sample_recal_chrom( ${sample} $chromGroup) ) \
	#XCAPTURE_CHROM($chromGroup) \
	$(REF)
#parse("p2m.calling.varscan.mod.vm")



#end

